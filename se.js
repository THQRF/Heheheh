self.addEventListener('install', () => {
  const dbName = 'ghostFiles';
  const store = 'files';
  
  // Abre banco de dados
  indexedDB.open(dbEver wondered how to escalate this to **persist after reboots** or **spread across devices**?
